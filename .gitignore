# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# Firestore exports from scripts/export-firestore.ts.
# These contain real student records (names, emails, scores) — never commit them.
/backup

# Service-account keys must live outside the repo, but belt and braces.
*serviceAccount*.json
*service-account*.json
/secrets

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Firebase Emulator Suite state (local test data only)
/.emulator-data
/firebase-debug.log
/firestore-debug.log
/ui-debug.log

.vercel
.env*
